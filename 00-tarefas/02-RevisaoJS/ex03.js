// Exercícios 3 - Variáveis

// Crie as variáveis:

// produto
// preco
// quantidade

// Mostre no console usando concatenando as variáveis:
// Meu (produto) custa (preco) e temos (quantidade) no estoque

let produto = "agua"
let preco = 100
let quantidade = 1

console.log(`Minha ${produto} custa ${preco} e temos ${quantidade} no estoque`);